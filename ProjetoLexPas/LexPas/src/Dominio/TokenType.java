package Dominio;

public enum TokenType {
    NUMBER,
    STRING,
    KEYWORD,
    IDENTIFIER,
    TRUE,
    FALSE,
    MATHOP,
    LOGICOP,
    DELIM,
    SYMBOL,
    EOF,
    UNKNOWN
}

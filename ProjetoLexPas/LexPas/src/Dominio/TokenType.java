package Dominio;

public enum TokenType {
    LITERAL,
    NUMBER,
    STRING,
    KEYWORD,
    IDENTIFIER,
    TRUE,
    FALSE,
    MATHOP,
    LOGICOP,
    DELIM,
    SYMBOL,
    EOF,
    UNKNOWN
}
